// Displays all sites visited by a user in the past period
DeviceNetworkEvents
|  where DeviceName == "desktopxx"
|  where InitiatingProcessFileName contains "msedge.exe" // You can modify the msedge.exe to the executable of any browser you want to review e.g. chrome.exe
|  project Timestamp, RemoteUrl, RemoteIP
sort by Timestamp desc
